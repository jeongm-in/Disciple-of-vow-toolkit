function getColor(button){
    return button.style.backgroundColor;
}


// 1. script to update labels
// when value changes, I can access top level key. Iterate through, grab key, grab id, and replace text.
// this can be a local script


// 2. firebase magic 

// 3. click, update color, sync with firebase db

// 4. reset to clear every color